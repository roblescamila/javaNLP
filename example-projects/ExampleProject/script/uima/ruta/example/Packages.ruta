PACKAGE uima.ruta.example;

TYPESYSTEM types.BibtexTypeSystem;

DECLARE Package,StartPackage,EndPackage;
W{REGEXP("package", true)-> StartPackage};

Document{-STARTSWITH(Package),ENDSWITH(SEMICOLON) -> MARK(EndPackage)};

StartPackage{ENDSWITH(SEMICOLON) -> MARK(Package)};

